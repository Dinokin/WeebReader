@model GeneralParametersModel

@{
    ViewData["Title"] = $"{Labels.General} - {Labels.Parameters}";
}

@section Headers
{
    <script>
    const siteNameRequiredLabel = "@ValidationMessages.SiteNameRequired";
    const googleAnalyticsCodeRequiredLabel = "@ValidationMessages.GoogleAnalyticsCodeRequired";
    const requestFailedLabel = "@OtherMessages.RequestFailed";
    const patchRoute = "@Url.Action("General")";
    </script>
    <script src="~/libs/weebreader/admin/parameter-general.js" asp-append-version="true"></script>
}

<form class="ui form" novalidate>
    <div class="ui message error"></div>
    <div class="field">
        <label>@Labels.SiteName</label>
        <input type="text" name="siteName" placeholder="@Labels.SiteName" asp-for="SiteName"/>
    </div>
    <div class="field">
        <label>@Labels.SiteDescription</label>
        <input type="text" name="siteDescription"  placeholder="@Labels.SiteDescription" asp-for="SiteDescription"/>
    </div>
    <div class="ui divider"></div>
    <div class="two fields">
        <div class="field">
            <label>@Labels.GoogleAnalytics</label>
            <div class="ui toggle checkbox">
                <input class="hidden" type="checkbox" name="googleAnalyticsEnabled" asp-for="GoogleAnalyticsEnabled"/>
                <label>@Labels.Enable</label>
            </div>
        </div>
        <div class="field">
            <label>@Labels.GoogleAnalyticsCode</label>
            <input type="text" name="googleAnalyticsCode" placeholder="@Labels.GoogleAnalyticsCode" asp-for="GoogleAnalyticsCode"/>
        </div>
    </div>
    <div class="ui divider"></div>
    <div class="field ov-auto">
        <div class="ui right floated black submit button ov-hidden">@Labels.UpdateParameters</div>
    </div>
</form>