@inject ParametersManager ParametersManager

@{
    ViewData["Title"] = Labels.About;
}

@section Headers
{
    <script src="~/libs/weebreader/main/about.js" asp-append-version="true"></script>
}

<div class="ui container">
    @if (await ParametersManager.GetValue<bool>(Parameter.Types.PageAboutEnabled))
    {
        <div class="ui raised segment">
            <div class="ui hidden divider"></div>
            <div class="ui content">
                @Html.Raw(await ParametersManager.GetValue<string>(Parameter.Types.PageAboutContent))
            </div>
        </div>   
    }
    @if (await ParametersManager.GetValue<bool>(Parameter.Types.PageAboutPatreonEnabled))
    {
        <div class="ui raised segment">
            <h4 class="ui header">@Labels.BecomeOurPatreon</h4>
            @Html.Raw(await ParametersManager.GetValue<string>(Parameter.Types.PageAboutPatreonNotice))
            <a class="ui fluid mini grey button" href="@(await ParametersManager.GetValue<string>(Parameter.Types.PageAboutPatreonLink))">
                <img class="ui centered small image" src="~/assets/patreon.png" alt="@Labels.Patreon"/>
            </a>
        </div>
    }
    @if (await ParametersManager.GetValue<bool>(Parameter.Types.PageAboutKofiEnabled))
    {
        <div class="ui raised segment">
            <h4 class="ui header">@Labels.BuyUsACoffee</h4>
            @Html.Raw(await ParametersManager.GetValue<string>(Parameter.Types.PageAboutKofiNotice))
            <a class="ui fluid mini grey button" href="@(await ParametersManager.GetValue<string>(Parameter.Types.PageAboutKofiLink))">
                <img class="ui centered small image" src="~/assets/kofi.png" alt="@Labels.KoFi"/>
            </a>
        </div>
    }
</div>